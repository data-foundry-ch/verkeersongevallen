# Production monolith (Render): API on same host as the map UI.
VITE_API_BASE=/api

# Optional: "back to marketing" link in the map chrome (if you add it in Lovable merge).
# VITE_MARKETING_URL=https://datafoundry.ch

VITE_MAP_STYLE=positron
